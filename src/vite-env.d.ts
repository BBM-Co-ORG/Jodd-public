/// <reference types="vite/client" />
// Types for Vite's `import.meta.env` (DEV/PROD/MODE). Added when App.svelte
// started gating the dev-only window-title stamp on `import.meta.env.DEV`;
// without this reference svelte-check reports
// "Property 'env' does not exist on type 'ImportMeta'".
